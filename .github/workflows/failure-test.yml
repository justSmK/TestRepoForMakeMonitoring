name: Failure Test

on:
  workflow_dispatch:

jobs:
  always-fail:
    runs-on: ubuntu-latest
    steps:
      - name: Print start message
        run: echo "Starting failure workflow"

      - name: Fail
        run: |
          echo "This job will fail"
          exit 1
